<% @user ||= current_user %>
<div class="stats">
  <%= link_to t('views.users.show.following_count', count:@user.followings.count), followings_user_path(@user) %>
  <%= link_to t('views.users.show.follower_count', count:@user.followers.count), followers_user_path(@user) %>
</div>
